MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/war_room?retryWrites=true&w=majority
MONGODB_DB=war_room

# Optional local JSONL replay file writes (Mongo replay is enabled automatically when MONGODB_URI is set)
OPERATION_REPLAY_WRITE_ENABLED=false
